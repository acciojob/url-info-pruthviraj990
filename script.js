function showUrlLength() {
  const currentUrl = location.href; // Get the full current URL
  const urlLength = currentUrl.length; // Get the length
  alert(`The length of the URL is: ${urlLength}`);
}
